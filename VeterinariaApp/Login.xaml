<Window x:Class="VeterinariaApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="350" Width="400"
        MinHeight="300" MinWidth="350"
        WindowStartupLocation="CenterScreen"
        Background="#f0f2f5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <Border Background="#4CAF50" Height="80" CornerRadius="10" VerticalAlignment="Top">
            <TextBlock Text="VeterinariaApp" Foreground="White" FontSize="28" FontWeight="Bold" 
                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>

        <!-- Formulario -->
        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="20">
                <Label Content="Usuario" FontWeight="Bold"/>
                <TextBox Name="txtUsuario" Height="30" FontSize="14"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="20,10,20,0">
                <Label Content="Contraseña" FontWeight="Bold"/>
                <PasswordBox Name="txtPassword" Height="30" FontSize="14"/>
            </StackPanel>

            <Button Grid.Row="2" Content="Entrar" Height="40" Width="150" HorizontalAlignment="Center" Margin="0,20,0,0"
                    Background="#2196F3" Foreground="White" FontWeight="Bold" Click="BtnEntrar_Click"/>
        </Grid>
    </Grid>
</Window>
